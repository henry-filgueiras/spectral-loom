{
  "$defs": {
    "AudioArtifact": {
      "additionalProperties": false,
      "description": "One audio file this project wrote, measured after writing it.\n\nMeasured *after*, not before. The numbers here describe the bytes on disk \u2014\nthe ones a person will actually hear and a later stage will actually read \u2014\nrather than an in-memory tensor that was quantized on its way out.",
      "properties": {
        "channels": {
          "maximum": 64,
          "minimum": 1,
          "title": "Channels",
          "type": "integer"
        },
        "duration_s": {
          "exclusiveMinimum": 0,
          "title": "Duration S",
          "type": "number"
        },
        "hash": {
          "pattern": "^sha256:[0-9a-f]{64}$",
          "title": "Hash",
          "type": "string"
        },
        "path": {
          "description": "Location relative to the repository root. A claim about where, not what.",
          "maxLength": 512,
          "minLength": 1,
          "title": "Path",
          "type": "string"
        },
        "peak": {
          "description": "Largest absolute sample value, observed.",
          "minimum": 0,
          "title": "Peak",
          "type": "number"
        },
        "rms": {
          "description": "Root mean square over the whole file, observed.",
          "minimum": 0,
          "title": "Rms",
          "type": "number"
        },
        "sample_rate_hz": {
          "exclusiveMinimum": 0,
          "title": "Sample Rate Hz",
          "type": "integer"
        }
      },
      "required": [
        "path",
        "hash",
        "duration_s",
        "sample_rate_hz",
        "channels",
        "peak",
        "rms"
      ],
      "title": "AudioArtifact",
      "type": "object"
    },
    "Diagnostic": {
      "additionalProperties": false,
      "description": "An engineering check, deliberately not an inferred stem.\n\nA reconstructed mix and a residual are arithmetic performed on the outputs.\nThey carry no model opinion, they are not evidence about the music, and\nnothing downstream may treat one as a track. They exist so that a person can\nhear what separation lost, and so that a number for it is on the record\nrather than in somebody's memory.",
      "properties": {
        "audio": {
          "anyOf": [
            {
              "$ref": "#/$defs/AudioArtifact"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The rendered diagnostic, where one was written."
        },
        "description": {
          "description": "What this diagnostic is, in one sentence.",
          "maxLength": 500,
          "minLength": 1,
          "title": "Description",
          "type": "string"
        },
        "id": {
          "pattern": "^[a-z0-9][a-z0-9._-]{2,63}$",
          "title": "Id",
          "type": "string"
        },
        "measurements": {
          "additionalProperties": {
            "$ref": "#/$defs/JsonValue"
          },
          "description": "Observed numbers. No thresholds, no verdicts.",
          "title": "Measurements",
          "type": "object"
        }
      },
      "required": [
        "id",
        "description"
      ],
      "title": "Diagnostic",
      "type": "object"
    },
    "JsonValue": {},
    "Provenance": {
      "additionalProperties": false,
      "description": "One producing stage, recorded so its output can be attributed and recomputed.\n\nA stage entry answers: which inputs, which tool at which exact revision, with\nwhich parameters, on which runtime, for how long, emitting which artifacts,\nand at which truth layer. Together with the input hashes it is also the\ncache key: an entry whose key cannot be recomputed is garbage, not a cache.",
      "properties": {
        "duration_ms": {
          "anyOf": [
            {
              "minimum": 0,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Duration Ms"
        },
        "input_hashes": {
          "additionalProperties": {
            "pattern": "^sha256:[0-9a-f]{64}$",
            "type": "string"
          },
          "description": "Role name to content hash for every input this stage consumed.",
          "title": "Input Hashes",
          "type": "object"
        },
        "output_hashes": {
          "additionalProperties": {
            "pattern": "^sha256:[0-9a-f]{64}$",
            "type": "string"
          },
          "description": "Role name to content hash for every artifact this stage emitted.",
          "title": "Output Hashes",
          "type": "object"
        },
        "parameters": {
          "additionalProperties": {
            "$ref": "#/$defs/JsonValue"
          },
          "description": "Parameters that affected the result, and therefore the cache key.",
          "title": "Parameters",
          "type": "object"
        },
        "runtime": {
          "anyOf": [
            {
              "maxLength": 200,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Runtime and backend actually used, e.g. 'cpython3.11 macos-arm64 mps'.",
          "title": "Runtime"
        },
        "stage": {
          "description": "Stage name, unique within a timeline, e.g. 'separation' or 'onsets.bass'.",
          "maxLength": 64,
          "pattern": "^[a-z][a-z0-9]*(\\.[a-z0-9_]+)*$",
          "title": "Stage",
          "type": "string"
        },
        "started_at": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Started At"
        },
        "tool": {
          "description": "Tool or model identity.",
          "maxLength": 200,
          "minLength": 1,
          "title": "Tool",
          "type": "string"
        },
        "tool_revision": {
          "description": "Exact revision of that tool: a version plus commit, never a branch.",
          "maxLength": 200,
          "minLength": 1,
          "title": "Tool Revision",
          "type": "string"
        },
        "truth_layer": {
          "$ref": "#/$defs/TruthLayer"
        }
      },
      "required": [
        "stage",
        "tool",
        "tool_revision",
        "truth_layer"
      ],
      "title": "Provenance",
      "type": "object"
    },
    "Separator": {
      "additionalProperties": false,
      "description": "Exactly which implementation, loading exactly which weights.\n\nBoth halves, because upstream versions them separately: a distribution\nidentity alone would not say which bytes were loaded, and a weights revision\nalone would not say which code interpreted them.",
      "properties": {
        "adapter": {
          "description": "This project's adapter name.",
          "maxLength": 64,
          "minLength": 1,
          "title": "Adapter",
          "type": "string"
        },
        "applied_with": {
          "description": "Symbol that performed the separation, e.g. demucs.apply.apply_model.",
          "maxLength": 200,
          "minLength": 1,
          "title": "Applied With",
          "type": "string"
        },
        "code_distribution": {
          "maxLength": 100,
          "minLength": 1,
          "title": "Code Distribution",
          "type": "string"
        },
        "code_sha256": {
          "description": "Digest the package index published for the exact artifact installed.",
          "pattern": "^[0-9a-f]{64}$",
          "title": "Code Sha256",
          "type": "string"
        },
        "code_version": {
          "maxLength": 50,
          "minLength": 1,
          "title": "Code Version",
          "type": "string"
        },
        "loaded_with": {
          "description": "Symbol that read the weights off disk, e.g. demucs.hf.load_safetensors_model.",
          "maxLength": 200,
          "minLength": 1,
          "title": "Loaded With",
          "type": "string"
        },
        "model_audio_channels": {
          "maximum": 64,
          "minimum": 1,
          "title": "Model Audio Channels",
          "type": "integer"
        },
        "model_sample_rate_hz": {
          "description": "The rate the loaded model works at, read off the model.",
          "exclusiveMinimum": 0,
          "title": "Model Sample Rate Hz",
          "type": "integer"
        },
        "model_signatures": {
          "description": "Signatures of the models in the bag, as upstream names them. `htdemucs` is a bag of exactly one model and its signature is part of what produced any stem.",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Model Signatures",
          "type": "array"
        },
        "sources": {
          "description": "The model's own output names, in the order the model emits them.",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Sources",
          "type": "array"
        },
        "weights_repo": {
          "maxLength": 200,
          "minLength": 1,
          "title": "Weights Repo",
          "type": "string"
        },
        "weights_revision": {
          "description": "Immutable upstream revision. Never a branch and never a tag.",
          "pattern": "^[0-9a-f]{40}$",
          "title": "Weights Revision",
          "type": "string"
        },
        "weights_variant": {
          "maxLength": 100,
          "minLength": 1,
          "title": "Weights Variant",
          "type": "string"
        }
      },
      "required": [
        "adapter",
        "code_distribution",
        "code_version",
        "code_sha256",
        "loaded_with",
        "applied_with",
        "weights_repo",
        "weights_revision",
        "weights_variant",
        "model_signatures",
        "model_sample_rate_hz",
        "model_audio_channels",
        "sources"
      ],
      "title": "Separator",
      "type": "object"
    },
    "SourceAudio": {
      "additionalProperties": false,
      "description": "The evidence-bearing artifact this timeline is about.",
      "properties": {
        "channels": {
          "anyOf": [
            {
              "maximum": 64,
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Channels"
        },
        "duration_s": {
          "description": "Observed duration of that audio.",
          "exclusiveMinimum": 0,
          "title": "Duration S",
          "type": "number"
        },
        "hash": {
          "description": "Hash of the exact audio bytes the stages consumed.",
          "pattern": "^sha256:[0-9a-f]{64}$",
          "title": "Hash",
          "type": "string"
        },
        "sample_rate_hz": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Sample Rate Hz"
        }
      },
      "required": [
        "hash",
        "duration_s"
      ],
      "title": "SourceAudio",
      "type": "object"
    },
    "Stem": {
      "additionalProperties": false,
      "description": "One output a separator produced, named the way the separator names it.\n\n``model_output`` is the single most misreadable field in this project, so it\nis not called ``instrument``. ``bass`` means **HTDemucs assigned this signal\nto its `bass` output**. It does not mean the file contains only bass, and it\ndoes not mean the source contained a bass.\n\nThe same care applies hardest to ``vocals``. Content appearing there may be\nvoice-like source material, model confusion, or leakage from another\ninstrument, and a stem that is near-silent is a failure to assign rather than\nproof that nobody sang.",
      "properties": {
        "audio": {
          "$ref": "#/$defs/AudioArtifact"
        },
        "clipped_samples": {
          "description": "Samples whose magnitude exceeded full scale and were clamped on write. Recorded rather than rescaled away: a silent gain change would corrupt every later comparison against the source.",
          "minimum": 0,
          "title": "Clipped Samples",
          "type": "integer"
        },
        "model_output": {
          "description": "The separator's own name for this output. Not a verified instrument.",
          "pattern": "^[a-z0-9][a-z0-9._-]{2,63}$",
          "title": "Model Output",
          "type": "string"
        },
        "non_finite_samples": {
          "description": "Samples the model emitted that were not finite. Nonzero is a real problem.",
          "minimum": 0,
          "title": "Non Finite Samples",
          "type": "integer"
        }
      },
      "required": [
        "model_output",
        "audio",
        "clipped_samples",
        "non_finite_samples"
      ],
      "title": "Stem",
      "type": "object"
    },
    "TruthLayer": {
      "description": "Where a value came from. These never collapse into one another.\n\n``requested``\n    Authored before the audio existed. A prompt, a seed, a target BPM.\n``observed``\n    Measurable from the artifact by anyone holding it. A duration, a hash.\n``inferred``\n    A named model's opinion at an exact revision, with a confidence.\n``corrected``\n    A human override, recorded as its own stage rather than as an edit that\n    erases the inference it replaced.",
      "enum": [
        "requested",
        "observed",
        "inferred",
        "corrected"
      ],
      "title": "TruthLayer",
      "type": "string"
    }
  },
  "$id": "https://spectral-loom.invalid/schemas/spectral-loom/separation-manifest/0.1.0.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "The record that makes one separation auditable and reusable.\n\nTwo jobs, and `docs/provenance.md` says they are the same information: it\nattributes the stems, and it *is* the cache key. Reuse is a comparison of\nthis document against a freshly computed key, followed by re-hashing every\nfile it claims to describe \u2014 because a manifest describing files that have\nsince changed is not a cache entry, it is a document making a false claim.\n\nThe truth layers stay apart. Hashes, durations, sample rates, channel counts,\npeaks and residuals are ``observed``: anyone holding the files can recompute\nthem. The producing stage is ``inferred``: HTDemucs at an exact revision had\nan opinion about which signal belonged to which of its outputs.\n\n**No human quality judgement belongs in here.** Whether the bass is actually\nisolated is gate 3, it is answered by ears, and it is recorded in a\n`SpecimenReview`, not in the output of the thing being judged.",
  "properties": {
    "cache_key": {
      "description": "Digest over `cache_key_inputs`. A run whose key differs is a different run.",
      "pattern": "^sha256:[0-9a-f]{64}$",
      "title": "Cache Key",
      "type": "string"
    },
    "cache_key_inputs": {
      "additionalProperties": {
        "$ref": "#/$defs/JsonValue"
      },
      "description": "Everything the key was computed from, so that a stale entry can be diagnosed rather than merely disbelieved.",
      "title": "Cache Key Inputs",
      "type": "object"
    },
    "diagnostics": {
      "items": {
        "$ref": "#/$defs/Diagnostic"
      },
      "title": "Diagnostics",
      "type": "array"
    },
    "generation_manifest_hash": {
      "description": "Hash of the generation manifest that attributes the separated bytes.",
      "pattern": "^sha256:[0-9a-f]{64}$",
      "title": "Generation Manifest Hash",
      "type": "string"
    },
    "provenance": {
      "items": {
        "$ref": "#/$defs/Provenance"
      },
      "minItems": 1,
      "title": "Provenance",
      "type": "array"
    },
    "review_hash": {
      "description": "Hash of the specimen review that accepted these bytes. Separation does not run on audio no human has accepted, and this is the receipt for that precondition.",
      "pattern": "^sha256:[0-9a-f]{64}$",
      "title": "Review Hash",
      "type": "string"
    },
    "schema_id": {
      "const": "spectral-loom/separation-manifest",
      "default": "spectral-loom/separation-manifest",
      "title": "Schema Id",
      "type": "string"
    },
    "schema_version": {
      "const": "0.1.0",
      "default": "0.1.0",
      "title": "Schema Version",
      "type": "string"
    },
    "separator": {
      "$ref": "#/$defs/Separator"
    },
    "source_audio": {
      "$ref": "#/$defs/SourceAudio",
      "description": "The exact bytes that were separated. Observed, and verified before the run."
    },
    "source_path": {
      "maxLength": 512,
      "minLength": 1,
      "title": "Source Path",
      "type": "string"
    },
    "specimen_id": {
      "pattern": "^[a-z0-9][a-z0-9._-]{2,63}$",
      "title": "Specimen Id",
      "type": "string"
    },
    "stems": {
      "items": {
        "$ref": "#/$defs/Stem"
      },
      "minItems": 1,
      "title": "Stems",
      "type": "array"
    },
    "warnings": {
      "description": "What the run emitted or had to do, including any explicit fallback.",
      "items": {
        "type": "string"
      },
      "title": "Warnings",
      "type": "array"
    }
  },
  "required": [
    "specimen_id",
    "source_audio",
    "source_path",
    "review_hash",
    "generation_manifest_hash",
    "separator",
    "stems",
    "cache_key",
    "cache_key_inputs",
    "provenance"
  ],
  "title": "SeparationManifest",
  "type": "object"
}
