{
  "$defs": {
    "CriterionResponse": {
      "description": "A reviewer's answer to one criterion.\n\n``UNCLEAR`` exists so that a reviewer who could not tell is not forced to\nsay \"no\". The distinction matters for the same reason it matters everywhere\nelse in this project: not detecting something and establishing its absence\nare different claims.",
      "enum": [
        "yes",
        "no",
        "unclear"
      ],
      "title": "CriterionResponse",
      "type": "string"
    },
    "HumanReview": {
      "additionalProperties": false,
      "description": "A human's judgement about an artifact's fitness for a purpose.\n\n**This is deliberately not one of the four truth layers**, and the omission\nis the careful part. ``requested``, ``observed``, ``inferred`` and\n``corrected`` classify *claims about a recording* by where the claim came\nfrom. \"I accept these bytes as this project's first experimental specimen\"\nis not a claim about the recording at all: it is a claim about the project.\nNothing here says the audio contains a bass, a tempo, or a key.\n\nSo a review is not a :class:`Provenance` entry either. A provenance entry\nrecords a stage so its output can be attributed and recomputed; a review\nemits no artifact and cannot be recomputed, and giving a person a\n``tool_revision`` would be a fiction. See ``archaeology/decisions/0010``.\n\nWhat the answers *do* record is what a named person perceived on a named\nday. ``bass-audible-and-exposed: yes`` means the reviewer heard something\nthey call an exposed bass. It does not establish that the source contains an\nelectric bass, and no downstream stage may read it as though it did.",
      "properties": {
        "accepted": {
          "description": "Whether the reviewer accepts these exact bytes for the purpose in `purpose`. Never a claim that any requested instrument, tempo, or key was established.",
          "title": "Accepted",
          "type": "boolean"
        },
        "criteria": {
          "description": "Every question put to the reviewer, with their answer.",
          "items": {
            "$ref": "#/$defs/ReviewCriterion"
          },
          "minItems": 1,
          "title": "Criteria",
          "type": "array"
        },
        "method": {
          "description": "How the artifact was reviewed, in enough detail to know what was not done.",
          "maxLength": 1000,
          "minLength": 1,
          "title": "Method",
          "type": "string"
        },
        "notes": {
          "anyOf": [
            {
              "maxLength": 4000,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Free prose. On a rejection this is where what was wrong goes, because a record that can only say yes is a marketing document.",
          "title": "Notes"
        },
        "purpose": {
          "description": "What acceptance would mean. Stated, so that it cannot be quietly widened.",
          "maxLength": 500,
          "minLength": 1,
          "title": "Purpose",
          "type": "string"
        },
        "reviewed_on": {
          "description": "The day they listened. A judgement is dated, not versioned.",
          "format": "date",
          "title": "Reviewed On",
          "type": "string"
        },
        "reviewer": {
          "description": "Who listened. A person, named.",
          "maxLength": 200,
          "minLength": 1,
          "title": "Reviewer",
          "type": "string"
        }
      },
      "required": [
        "reviewer",
        "reviewed_on",
        "method",
        "criteria",
        "accepted",
        "purpose"
      ],
      "title": "HumanReview",
      "type": "object"
    },
    "ReviewCriterion": {
      "additionalProperties": false,
      "description": "One question a reviewer was asked, and what they answered.\n\nThe question travels with the answer on purpose. A bare ``{\"bass\": \"yes\"}``\nread two years later is uninterpretable, and a criterion whose wording\nchanged silently would make two reviews look comparable when they are not.",
      "properties": {
        "id": {
          "description": "Stable identifier for this criterion within its question set.",
          "pattern": "^[a-z0-9][a-z0-9._-]{2,63}$",
          "title": "Id",
          "type": "string"
        },
        "notes": {
          "anyOf": [
            {
              "maxLength": 2000,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The reviewer's own qualification of their answer, if they gave one.",
          "title": "Notes"
        },
        "question": {
          "description": "Exactly what the reviewer was asked, in the wording they were asked it.",
          "maxLength": 500,
          "minLength": 1,
          "title": "Question",
          "type": "string"
        },
        "response": {
          "$ref": "#/$defs/CriterionResponse"
        }
      },
      "required": [
        "id",
        "question",
        "response"
      ],
      "title": "ReviewCriterion",
      "type": "object"
    },
    "ReviewedArtifact": {
      "additionalProperties": false,
      "description": "One file that was actually in the exhibit a reviewer listened to.\n\nA verdict about \"the separation\" is worth nothing unless the separation can\nbe identified. A directory can be regenerated under a different revision, a\ndifferent backend, or a different parameter and still answer to the same\nname, so the acceptance names **every file by hash**: a later run whose\nbytes differ is a different separation and inherits nothing.\n\n``kind`` keeps the same distinction the separation manifest keeps.\n``model_output`` is an assignment HTDemucs made; ``diagnostic`` is\narithmetic performed on those assignments and carries no model opinion;\n``source`` is the evidence they are all claims about.",
      "properties": {
        "hash": {
          "description": "The bytes that were actually heard.",
          "pattern": "^sha256:[0-9a-f]{64}$",
          "title": "Hash",
          "type": "string"
        },
        "kind": {
          "enum": [
            "source",
            "model_output",
            "diagnostic"
          ],
          "title": "Kind",
          "type": "string"
        },
        "name": {
          "description": "The artifact's own name, e.g. 'bass' or 'residual'.",
          "pattern": "^[a-z0-9][a-z0-9._-]{2,63}$",
          "title": "Name",
          "type": "string"
        },
        "path": {
          "description": "Where it was when it was reviewed. A claim about where, not what.",
          "maxLength": 512,
          "minLength": 1,
          "title": "Path",
          "type": "string"
        }
      },
      "required": [
        "name",
        "kind",
        "path",
        "hash"
      ],
      "title": "ReviewedArtifact",
      "type": "object"
    },
    "Separator": {
      "additionalProperties": false,
      "description": "Exactly which implementation, loading exactly which weights.\n\nBoth halves, because upstream versions them separately: a distribution\nidentity alone would not say which bytes were loaded, and a weights revision\nalone would not say which code interpreted them.",
      "properties": {
        "adapter": {
          "description": "This project's adapter name.",
          "maxLength": 64,
          "minLength": 1,
          "title": "Adapter",
          "type": "string"
        },
        "applied_with": {
          "description": "Symbol that performed the separation, e.g. demucs.apply.apply_model.",
          "maxLength": 200,
          "minLength": 1,
          "title": "Applied With",
          "type": "string"
        },
        "code_distribution": {
          "maxLength": 100,
          "minLength": 1,
          "title": "Code Distribution",
          "type": "string"
        },
        "code_sha256": {
          "description": "Digest the package index published for the exact artifact installed.",
          "pattern": "^[0-9a-f]{64}$",
          "title": "Code Sha256",
          "type": "string"
        },
        "code_version": {
          "maxLength": 50,
          "minLength": 1,
          "title": "Code Version",
          "type": "string"
        },
        "loaded_with": {
          "description": "Symbol that read the weights off disk, e.g. demucs.hf.load_safetensors_model.",
          "maxLength": 200,
          "minLength": 1,
          "title": "Loaded With",
          "type": "string"
        },
        "model_audio_channels": {
          "maximum": 64,
          "minimum": 1,
          "title": "Model Audio Channels",
          "type": "integer"
        },
        "model_sample_rate_hz": {
          "description": "The rate the loaded model works at, read off the model.",
          "exclusiveMinimum": 0,
          "title": "Model Sample Rate Hz",
          "type": "integer"
        },
        "model_signatures": {
          "description": "Signatures of the models in the bag, as upstream names them. `htdemucs` is a bag of exactly one model and its signature is part of what produced any stem.",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Model Signatures",
          "type": "array"
        },
        "sources": {
          "description": "The model's own output names, in the order the model emits them.",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Sources",
          "type": "array"
        },
        "weights_repo": {
          "maxLength": 200,
          "minLength": 1,
          "title": "Weights Repo",
          "type": "string"
        },
        "weights_revision": {
          "description": "Immutable upstream revision. Never a branch and never a tag.",
          "pattern": "^[0-9a-f]{40}$",
          "title": "Weights Revision",
          "type": "string"
        },
        "weights_variant": {
          "maxLength": 100,
          "minLength": 1,
          "title": "Weights Variant",
          "type": "string"
        }
      },
      "required": [
        "adapter",
        "code_distribution",
        "code_version",
        "code_sha256",
        "loaded_with",
        "applied_with",
        "weights_repo",
        "weights_revision",
        "weights_variant",
        "model_signatures",
        "model_sample_rate_hz",
        "model_audio_channels",
        "sources"
      ],
      "title": "Separator",
      "type": "object"
    },
    "SourceAudio": {
      "additionalProperties": false,
      "description": "The evidence-bearing artifact this timeline is about.",
      "properties": {
        "channels": {
          "anyOf": [
            {
              "maximum": 64,
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Channels"
        },
        "duration_s": {
          "description": "Observed duration of that audio.",
          "exclusiveMinimum": 0,
          "title": "Duration S",
          "type": "number"
        },
        "hash": {
          "description": "Hash of the exact audio bytes the stages consumed.",
          "pattern": "^sha256:[0-9a-f]{64}$",
          "title": "Hash",
          "type": "string"
        },
        "sample_rate_hz": {
          "anyOf": [
            {
              "exclusiveMinimum": 0,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Sample Rate Hz"
        }
      },
      "required": [
        "hash",
        "duration_s"
      ],
      "title": "SourceAudio",
      "type": "object"
    },
    "SupplementaryListening": {
      "additionalProperties": false,
      "description": "A second opinion that is context, not a reviewer.\n\nRecorded because leaving it out would misrepresent how the judgement was\nreached, and typed separately from :class:`HumanReview` because a review has\nexactly one author and this is not it. Nothing mechanical reads this field,\nand no downstream stage may treat it as an acceptance.",
      "properties": {
        "listener": {
          "description": "Who, or what, assessed it.",
          "maxLength": 200,
          "minLength": 1,
          "title": "Listener",
          "type": "string"
        },
        "nature": {
          "description": "What kind of assessment this is and how this project came by it, in enough detail that a reader does not have to assume it was produced the same way the reviewer's was.",
          "maxLength": 500,
          "minLength": 1,
          "title": "Nature",
          "type": "string"
        },
        "summary": {
          "description": "What it said, as reported to this project.",
          "maxLength": 2000,
          "minLength": 1,
          "title": "Summary",
          "type": "string"
        }
      },
      "required": [
        "listener",
        "nature",
        "summary"
      ],
      "title": "SupplementaryListening",
      "type": "object"
    }
  },
  "$id": "https://spectral-loom.invalid/schemas/spectral-loom/separation-review/0.1.0.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "The tracked record that one human judged one exact separation.\n\nThe layer below :class:`SpecimenReview`, and it exists for the same reason.\n``decision:10`` established that a specimen id names an *intent* and cannot\nstand in for the bytes a person heard. A separation is one step worse: it is\nidentified by a directory that will be reused by the next run, so\n`corpus/derived/<specimen>/separation/` can be regenerated on another\nbackend, at another revision, or with another parameter and still resolve \u2014\nto stems nobody has heard. This document therefore names the separation\nmanifest's own content hash, the separator's exact code and weight identity,\nand every artifact that was in the exhibit, each by hash.\n\n**The human judgement is still not a truth layer.** \"These stems are fit to\nbe evidence inputs for activity and onset inference\" is a claim about the\nproject, not about the recording, exactly as in ``decision:10``. What the\nreviewer perceived in a model output is a fact about their perception of a\nmodel's assignment, and never a fact about what the source contained: an\noutput in which nothing meaningful was heard is a failure to assign, and the\ncontract will not let that be written down as an absence in the music.",
  "properties": {
    "review": {
      "$ref": "#/$defs/HumanReview"
    },
    "reviewed_artifacts": {
      "description": "Every file in the exhibit that was listened to, each named by its hash.",
      "items": {
        "$ref": "#/$defs/ReviewedArtifact"
      },
      "minItems": 1,
      "title": "Reviewed Artifacts",
      "type": "array"
    },
    "schema_id": {
      "const": "spectral-loom/separation-review",
      "default": "spectral-loom/separation-review",
      "title": "Schema Id",
      "type": "string"
    },
    "schema_version": {
      "const": "0.1.0",
      "default": "0.1.0",
      "title": "Schema Version",
      "type": "string"
    },
    "separation_cache_key": {
      "description": "The manifest's own cache key, copied so the two can be compared later.",
      "pattern": "^sha256:[0-9a-f]{64}$",
      "title": "Separation Cache Key",
      "type": "string"
    },
    "separation_manifest_hash": {
      "description": "Hash of the separation manifest document itself, which is this separation's identity. A regenerated separation is a different manifest and a different hash, and it does not inherit this acceptance.",
      "pattern": "^sha256:[0-9a-f]{64}$",
      "title": "Separation Manifest Hash",
      "type": "string"
    },
    "separation_manifest_path": {
      "maxLength": 512,
      "minLength": 1,
      "title": "Separation Manifest Path",
      "type": "string"
    },
    "separator": {
      "$ref": "#/$defs/Separator",
      "description": "Which implementation loaded which weights, copied verbatim so the reviewed separation stays attributable from a clean clone where the manifest is absent."
    },
    "source_audio": {
      "$ref": "#/$defs/SourceAudio",
      "description": "The accepted source these stems were separated from. Observed."
    },
    "specimen_id": {
      "pattern": "^[a-z0-9][a-z0-9._-]{2,63}$",
      "title": "Specimen Id",
      "type": "string"
    },
    "specimen_review_hash": {
      "description": "Hash of the gate 2 review that accepted the source. A separation of unaccepted bytes could never have been produced, and this is the receipt for that.",
      "pattern": "^sha256:[0-9a-f]{64}$",
      "title": "Specimen Review Hash",
      "type": "string"
    },
    "supplementary": {
      "description": "Second opinions, recorded as context. `review` has exactly one author and these are not it; nothing mechanical reads them.",
      "items": {
        "$ref": "#/$defs/SupplementaryListening"
      },
      "title": "Supplementary",
      "type": "array"
    }
  },
  "required": [
    "specimen_id",
    "source_audio",
    "specimen_review_hash",
    "separation_manifest_path",
    "separation_manifest_hash",
    "separation_cache_key",
    "separator",
    "reviewed_artifacts",
    "review"
  ],
  "title": "SeparationReview",
  "type": "object"
}
